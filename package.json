{
  "name": "cc-docs",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "scrape": "bun run index.ts",
    "blog:write": "bun run blog-writer.ts",
    "blog:build": "bun run blog/build.ts",
    "blog": "bun run blog:write && bun run blog:build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@openrouter/ai-sdk-provider": "^2.3.1",
    "ai": "^6.0.116",
    "eta": "^4.5.1",
    "node-html-parser": "^7.1.0"
  }
}
